@model IEnumerable<Identity.Models.Models.IFCFile
     >

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAll.cshtml";

}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FileName)
            </th>
            @*<th>
                    @Html.DisplayNameFor(model => model.StaticFilePath)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.RelativeFilePath)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.UploadedTime)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.ApplicationUser)
                </th>*@
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.FileName)
                </td>
                @*<td>
                        @Html.DisplayFor(modelItem => item.StaticFilePath)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.RelativeFilePath)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.UploadedTime)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ApplicationUser.Id)
                    </td>*@
                <td>
                    <a asp-action="Details" asp-route-id="@item.Id">Open</a> |
                    <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
                    <a asp-action="Download" asp-route-id="@item.Id">Download</a>
                </td>
            </tr>
        }
    </tbody>
</table>
